import { NextResponse } from 'next/server'; import { studioCookieName, studioToken } from '@/lib/studio-auth';
export async function POST(request: Request) { const { password } = await request.json(); if (!process.env.STUDIO_PASSWORD || password !== process.env.STUDIO_PASSWORD) return NextResponse.json({ error: 'Incorrect password.' }, { status: 401 }); const response = NextResponse.json({ ok: true }); response.cookies.set(studioCookieName(), studioToken(), { httpOnly: true, secure: process.env.NODE_ENV === 'production', sameSite: 'strict', path: '/', maxAge: 60 * 60 * 12 }); return response; }
