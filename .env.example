# Set these in Vercel → Project → Settings → Environment Variables
STUDIO_PASSWORD=use-a-long-unique-password
STUDIO_SECRET=use-a-second-long-random-secret
# Automatically supplied after you create/connect a Vercel Blob store
BLOB_READ_WRITE_TOKEN=
# Optional: digits only, country code included. Example: 919876543210
NEXT_PUBLIC_WHATSAPP_NUMBER=
